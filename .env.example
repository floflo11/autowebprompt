# ============================================================
# autowebprompt - Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# All variables are OPTIONAL — the tool works without them.

# --- S3 Storage (optional) ---
# Required only if you want to upload artifacts to AWS S3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_S3_BUCKET=your_bucket_name

# --- Database (optional) ---
# Required only if you want to store results in PostgreSQL
# Supports any PostgreSQL-compatible DB (Neon, Supabase, RDS, local)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- Neon Database Provisioning (optional) ---
# API key from https://console.neon.tech/app/settings/api-keys
# Used by `autowebprompt db init` to create a free PostgreSQL database
# NEON_API_KEY=your_neon_api_key

# --- Browser ---
# Chrome CDP port (default: 9222)
# CDP_PORT=9222
